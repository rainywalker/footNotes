# footNotes
tinymce Editor 4.x / 5.x FootNotes Plugin.
> **JQuery is required**

#### tinymce version 4.x and 5.x support



### Usage
    tinymce.init({
      selector: 'textarea',
      plugins: [
        'footnotes'
      ],
      toolbar: 'footnotes',
    });
